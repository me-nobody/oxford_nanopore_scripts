process {
    executor = 'slurm'
    clusterOptions = {
            "--account ${System.getenv("SLURM_JOB_ACCOUNT")} " +    
            "--qos ${task.time <= 10.m ? 'bbshort' : 'bbgpu'}"  
    }
}

executor {
    queueSize = 60            
    submitRateLimit = '1sec'  
}